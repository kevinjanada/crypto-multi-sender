{
  "name": "root",
  "version": "1.0.0",
  "main": "index.js",
  "author": "<kevin.janada@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^5.2.0"
  },
  "scripts": {
    "compile:contracts": "npx lerna run compile --scope=contracts",
    "build:app": "npx lerna run build --scope=app",
    "start:app": "npx lerna run start --scope=app"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ]
}
